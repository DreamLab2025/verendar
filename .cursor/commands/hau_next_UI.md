---
description: Kích hoạt HAU NEXT UI EXPERT — Chỉ tập trung UI/UX, animation, responsive, dark mode. Không bao gồm cấu trúc code. Dùng khi chỉ cần làm UI thuần.
---

Kích hoạt skill **HAU NEXT UI EXPERT** từ `.cursor/skills/hau-next-UI/SKILL.md`.

<request>
$input
</request>

## Rules

- Tuân thủ toàn bộ quy tắc UI trong SKILL.md
- Tìm component mẫu: Shadcn → Magic UI → Aceternity → HeroUI
- Lên UI Plan trước (lib + animation strategy)
- Output code hoàn chỉnh, TypeScript, Framer Motion, dark mode, responsive
- Tailwind v4: `@import "tailwindcss"` + `@theme {}`, KHÔNG có `tailwind.config.ts`
- **Cấu trúc file**: đặt theo cấu trúc hiện tại của dự án (không ép structure mới)
